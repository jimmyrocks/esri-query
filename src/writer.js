const fs = require('fs');

var Writer = function(filename, type, options) {
  // Types
  var types = {
    'file': true,
    'geopackage': true,
  };

  return {
    'create': function(options){},
    'append': function(options){},
    'save': function(options){},
    'close': function(options){}
  }'
};
